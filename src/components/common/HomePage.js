import React, { PropTypes } from 'react';

const Home = () => {
  return (
    <div>
      <h1>Redux Todo App</h1>
      <p>Learning to bootstrap a React & Redux app</p>
    </div>
  );
};

export default Home;
